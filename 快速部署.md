# 🚀 快速部署到云端（3步搞定）

## ✅ 准备工作已完成
- `requirements.txt` ✓
- `.gitignore` ✓  
- `app.py` ✓
- 数据文件 ✓

---

## 🎯 开始部署（跟着做就行）

### 第1步：初始化Git（在终端运行）

```bash
# 方法1：运行初始化脚本（推荐）
./init_git.sh

# 方法2：手动运行命令
cd /Users/qrebecca/china_investment_tracker
git init
git add .
git commit -m "Initial commit: Belt and Road Investment Tracker"
```

---

### 第2步：上传到GitHub

#### 2.1 创建GitHub仓库
1. 打开浏览器访问：https://github.com/new
2. 填写：
   - Repository name: `china-investment-tracker`
   - Description: `Belt and Road Investment Data Analysis`
   - 选 Public 或 Private（都可以）
   - **其他都不勾选！**
3. 点击绿色按钮 "Create repository"

#### 2.2 推送代码
GitHub页面会显示命令，复制运行即可。或直接运行（记得替换YOUR_USERNAME）：

```bash
git remote add origin https://github.com/YOUR_USERNAME/china-investment-tracker.git
git branch -M main
git push -u origin main
```

**如果要求密码**：
- 访问 https://github.com/settings/tokens
- 点击 "Generate new token (classic)"
- 勾选 "repo"
- 生成后复制，粘贴到密码框

---

### 第3步：一键部署

1. 打开：https://share.streamlit.io/
2. 点击 "Sign in with GitHub" 登录
3. 点击 "New app"
4. 填写：
   - Repository: 选择你的 `china-investment-tracker`
   - Branch: `main`
   - Main file: `app.py`
5. 点击 "Deploy!"

等2-3分钟，搞定！🎉

---

## 🌐 完成后

你会得到一个链接，比如：
```
https://yourname-china-investment-tracker.streamlit.app
```

**这个链接可以：**
- ✅ 分享给任何人
- ✅ 不需要登录
- ✅ 手机也能访问
- ✅ 支持中英文切换

---

## 🔄 以后更新代码

```bash
git add .
git commit -m "更新说明"
git push
```

推送后，Streamlit会自动更新应用！

---

## 📚 详细教程

- 完整中文教程：`DEPLOYMENT_STEPS_CN.md`
- 英文文档：`README_DEPLOY.md`

---

## ❓ 遇到问题？

常见问题都在 `DEPLOYMENT_STEPS_CN.md` 里有解决方案。

或者直接问我！😊

